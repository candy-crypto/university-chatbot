# If you use Weaviate Cloud, the docs say you need the REST endpoint URL 
# and admin API key, and the client can configure gRPC automatically 
# from the REST endpoint
# Documentation: 
# https://docs.weaviate.io/weaviate/quickstart

OPENAI_API_KEY=
MOCK_OPENAI=false

OPENAI_CHAT_MODEL=gpt-5-mini
JUDGE_MODEL=gpt-5.4
OPENAI_EMBED_MODEL=text-embedding-3-small
TOP_K=5

DATABASE_URL=postgresql://user:pass@host:5432/univeristy_chatbot

# Weaviate connection mode: local or cloud
WEAVIATE_MODE=local

# Local Weaviate
WEAVIATE_HTTP_HOST=localhost
WEAVIATE_HTTP_PORT=8080
WEAVIATE_GRPC_HOST=localhost
WEAVIATE_GRPC_PORT=50051

# Weaviate Cloud
#WEAVIATE_URL=https://your-cluster.weaviate.network
#WEAVIATE_API_KEY=your_weaviate_api_key

# Collection name
WEAVIATE_COLLECTION=DepartmentChunk

# Catalog ingestion
CATALOG_PDF_PATH=25-26_New_Mexico_State_University_-_Las_Cruces.pdf
CATALOG_YEAR=2025-2026
CATALOG_DEPARTMENT_ID=cs
# Path to folder containing nmsu_catalog_chunker.py (defaults to current directory)
CATALOG_SCRIPTS_DIR=.
